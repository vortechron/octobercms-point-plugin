# ===================================
#  Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: vortechron.point::lang.category.name
        searchable: true
        relation: post
        select: title
    point:
        label: Point
    created_at:
        label: vortechron.point::lang.post.created
        type: date
